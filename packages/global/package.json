{
    "name": "@nyoomy/global",
    "version": "0.1.0",
    "private": true,
    "description": "Constant values referenced across the Nyoomy ecosystem.",
    "author": "",
    "main": "src/index.ts",
    "scripts": {
        "build": "tsc -b tsconfig.json",
        "build:watch": "tsc -b --watch",
        "test": "jest --detectOpenHandles"
    },
    "dependencies": {
        "@types/dotenv-webpack": "^7.0.2",
        "class-transformer": "^0.4.0",
        "class-validator": "^0.13.1",
        "dotenv": "^10.0.0",
        "reflect-metadata": "^0.1.13"
    },
    "devDependencies": {
        "jest": "^26.6.3"
    },
    "peerDependencies": {
        "typescript": "^4.2.4"
    }
}

mutation Register(
    $displayName: String!
    $email: String!
    $username: String!
    $password: String!
) {
    registerUser(
        displayName: $displayName
        email: $email
        username: $username
        password: $password
    ) {
        id
    }
}

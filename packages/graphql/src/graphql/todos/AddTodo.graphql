mutation AddTodo($input: AddTodoInput!) {
    addTodo(input: $input) {
        id
        title
        description
        isCompleted
        isArchived
        repeatWeekdays
        tags {
            id
            label
            description
            color
            icon
        }
    }
}

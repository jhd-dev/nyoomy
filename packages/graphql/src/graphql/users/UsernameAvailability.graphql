query UsernameAvailability($input: UsernameAvailabilityArgs!) {
    usernameAvailability(input: $input) {
        attemptedInput
        fieldName
        isAvailable
        alternatives
        timeChecked
    }
}
